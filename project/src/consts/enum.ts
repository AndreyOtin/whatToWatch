import exp from 'constants';

export enum AppRoute {
  Root = '/',
  Favorites = '/favorites',
  Login = '/login',
  Offer = '/offer/:id',
  City = '/city/:city',
}

export enum MaxElementCount {
  FilmCard = 8
}
