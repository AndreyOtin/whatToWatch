export enum AppRoute {
  Root = '/',
  Favorites = '/favorites',
  Login = '/login',
  Player = '/player/:id',
  Film = '/film/:film',
}

export enum MaxElementCount {
  FilmCard = 8,
  ToastError = 1,
}
